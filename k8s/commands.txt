./kafka-console-consumer \
     --bootstrap-server localhost:32100 \
     --topic trace \
     --key-deserializer org.apache.kafka.common.serialization.StringDeserializer \
     --value-deserializer org.apache.kafka.connect.json.JsonDeserializer \
     --from-beginning \
     --property print.key=true


kubectl run kafka-producer -ti --image=quay.io/strimzi/kafka:latest-kafka-2.6.0 --rm=true --restart=Never -- bin/kafka-console-producer.sh --broker-list cluster-name-kafka-bootstrap:9092 --topic my-topic
kubectl run kafka-consumer -ti --image=quay.io/strimzi/kafka:latest-kafka-2.6.0 --rm=true --restart=Never -- bin/kafka-console-consumer.sh --bootstrap-server cluster-name-kafka-bootstrap:9092 --topic my-topic --from-beginning


echo "{\"id\":\"foo\",\"lat\":1.0,\"lon\":1.0}" | kubectl exec -i my-cluster-kafka-0 -c kafka -- \
    bin/kafka-console-producer.sh --broker-list localhost:9092 --topic unmoved

echo "{\"mover_id\":\"bar6\",\"mover_lat\":1.0,\"mover_lon\":1.0}" | kubectl exec -i my-cluster-kafka-0 -c kafka -- \
    bin/kafka-console-producer.sh --broker-list localhost:9092 --topic trace

kubectl exec -i my-cluster-kafka-0 -c kafka -- \
    bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 \
    --from-beginning --property print.key=true \
    --topic trace

kubectl exec -i my-cluster-kafka-0 -c kafka -- \
    bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 \
    --from-beginning --property print.key=true \
    --value-deserializer org.apache.kafka.common.serialization.LongDeserializer \
    --topic geohash-averager

kubectl exec -i my-cluster-kafka-0 -c kafka -- \
    bin/kafka-topics.sh --list --bootstrap-server localhost:9092 

kubectl exec -i my-cluster-kafka-0 -c kafka -- \
    bin/kafka-topics.sh --describe \
    --bootstrap-server localhost:9092 \
    --topic track

kubectl exec -i my-cluster-kafka-0 -c kafka -- \
    bin/kafka-configs.sh --describe \
    --bootstrap-server localhost:9092 \
    --entity-type topics \
    --entity-name track

kubectl exec -i my-cluster-kafka-0 -c kafka -- \
    bin/kafka-configs.sh --alter \
    --bootstrap-server localhost:9092 \
    --entity-type topics \
    --entity-name arrival-raw \
    --add-config message.timestamp.type=LogAppendTime