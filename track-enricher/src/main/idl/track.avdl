@namespace("guru.bonacci.timesup.model")
protocol Tracks {

  record TrackGeo {
    string TRACKING_NUMBER;
    string MOVER_ID;
    string UNMOVED_ID;
    string UNMOVED_GEOHASH;
    double UNMOVED_LAT;
    double UNMOVED_LON;
  }

  record Track {
    string TRACKING_NUMBER;
    string MOVER_ID;
    string UNMOVED_ID;
  }
}
